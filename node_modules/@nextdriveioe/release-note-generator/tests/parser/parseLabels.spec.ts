import { parseLabels } from "../../src/parser/parseLabels";

describe("parseLabels spec", () => {
    it("should parseLabels proper", () => {
        parseLabels("");
        const result = parseLabels("[EC-893] add changePassword API\\n\\nlabels=api,ui ,Auto merge #134")
        expect(result).toMatchSnapshot();
    });
});
