import { parseJiraIssue } from "../../src/parser/parseJiraIssue";

describe("parseJiraIssue spec", () => {
    it("should parserJiraIssue proper", () => {
        const result = parseJiraIssue("[EC-893] [EC-894] add changePassword API\\n\\nAuto merge #134\"");
        expect(result[0]).toBe("EC-893");
        expect(result[1]).toBe("EC-894");
    });
});
