import { Arguments, Argv, CommandModule } from "yargs";
declare type CommandArguments = {
    repo: string;
    token: string;
    tag: string;
    jira: string;
    username: string;
    password: string;
};
export declare class SyncJiraReleaseCommand implements CommandModule {
    command: string;
    deprecated: boolean;
    describe: string;
    builder(args: Argv<CommandArguments>): any;
    private args;
    private jiraOptions;
    handler(args: Arguments<CommandArguments>): Promise<void>;
}
export declare function getStatusTransitionId(baseUrl: string, jiraOptions: object, issue: string, statusName: string): Promise<string | null>;
export declare function updateStatus(baseUrl: string, jiraOptions: object, issue: string, statusName: string): Promise<void>;
export {};
