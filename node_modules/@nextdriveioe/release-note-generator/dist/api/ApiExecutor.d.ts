import { GithubCompareResponse } from "../types/GithubCompareResponse";
declare type ApiExecutorConfig = {
    repo: string;
    token: string;
};
export declare class ApiExecutor {
    private config;
    constructor(config: ApiExecutorConfig);
    compare(startSha: string, endSha: string, repo?: string): Promise<GithubCompareResponse>;
    get headers(): {
        headers: {
            Authorization: string;
        };
    };
}
export {};
