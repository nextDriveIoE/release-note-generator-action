import { ApiExecutor } from "../api/ApiExecutor";
import { Commit, GithubCompareResponse } from "../types/GithubCompareResponse";
export declare type DiffVersion = {
    packageName: string;
    repo: string;
    label: string;
    from: string;
    to: string;
    changed: boolean;
};
export declare class DiffVersionService {
    private apiExecutor;
    constructor(apiExecutor: ApiExecutor);
    getVersions(res: GithubCompareResponse, packagePath: string, deepPackages: string[], repos: string[], labels: string[], tagPrefix: string[]): Array<DiffVersion>;
    getPackagesCommits(versions: Array<DiffVersion>): Promise<Array<Commit>>;
}
